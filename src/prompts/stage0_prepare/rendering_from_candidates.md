Below are entity candidates from a $source_name document, each with the context it appears in.

For each candidate, decide whether it contains a true proper-name nucleus that must survive translation exactly, or whether it is a descriptive named expression whose meaningful words should be translated into natural $target_name. Personal names and opaque name nuclei are preserved; titles, honorifics, ranks, realms, sect/faction labels, techniques, artefact names, and descriptive place or organisation names are not automatically preserved. If a mixed phrase contains both, return only the proper-name nucleus as the preserve entry.$genre_line

CANDIDATES:
$listing
